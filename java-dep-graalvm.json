{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": ["Settings for Java dependencies: GraalVM"],
  "java": {
    "packageRules": [
      {
        "description": ["graalvm", "Group dependencies"],
        "packagePatterns": ["^org\\.graalvm[.:].+"],
        "groupName": "org.graalvm"
      }
    ]
  }
}
